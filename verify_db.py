from app.database import SessionLocal; from app.models import ScanTask; import sqlalchemy; from sqlalchemy import inspect; engine = SessionLocal().bind; inspector = inspect(engine); print("Tables:", inspector.get_table_names()); if "scan_tasks" in inspector.get_table_names(): print("ScanTask columns:", [c["name"] for c in inspector.get_columns("scan_tasks")]); print("Database schema verified") else: print("scan_tasks table not found")
