# 网络设备发现平台 · Claude Code 开发上下文

> **最后更新**: 2026-05-14 | **当前版本**: v0.9.2 | **状态**: 已发布 (CSV导出)

---

## 🚨 环境强制声明（不遵守 = 任务作废）

> **你的运行环境是本地开发机 (Windows)，但项目只在远程服务器上！**
> **📍 唯一合法的开发目录**: `192.168.88.94:/root/network-discovery`
> **🔑 唯一合法的操作方式**: 通过 `ssh root@192.168.88.94` 执行所有命令

---

## 📊 当前状态 (v0.9.2)

### ✅ 已完成
*   **v0.9.0**: 定时扫描配置页面、Schedules CRUD API、SQLite WAL 模式、全局扫描锁。
*   **v0.9.1**: 仪表盘数据可视化（Chart.js 环形图、条形图）。
*   **v0.9.2**: 资产数据导出功能
    - GET /api/assets/export 接口
    - CSV 格式导出（UTF-8 BOM）
    - 前端导出 CSV按钮

### ⏭️ 下一步计划
*   **v0.10.0 候选**: Excel 导出（需安装 openpyxl）、资产趋势图表。

---

## 📁 项目结构
```
/root/network-discovery/
├── app/routers/assets.py  # 资产路由（含导出接口）
├── assets.html            # 资产清单页（含导出按钮）
└── ...
```

---

## 🛠️ 开发规范

1. **Git 连续性**: 每次 Commit 前必须更新 CLAUDE.md。
2. **路由顺序**: FastAPI 固定路径必须放在动态路径之前。
