# 网络设备发现平台 · Claude Code 开发上下文

> **最后更新**: 2026-05-15 | **当前版本**: v0.9.3 | **状态**: 已发布 (稳定性专项)

---

## 🚨 环境强制声明（不遵守 = 任务作废）

> **你的运行环境是本地开发机 (Windows)，但项目只在远程服务器上！**
> **📍 唯一合法的开发目录**: `192.168.88.94:/root/network-discovery`
> **🔑 唯一合法的操作方式**: 通过 `ssh root@192.168.88.94` 执行所有命令

---

## 📊 当前状态 (v0.9.3)

### ✅ v0.9.3 更新内容
*   **Bug #1**: 导航链接已正确（/assets.html），无需修复
*   **Bug #2**: JS 版本号改为动态获取，移除硬编码
*   **Bug #3**: Bento Grid 布局改为 4 列（桌面端）
*   **优化 #1**: 扫描输入框校验（空值/格式/超大网段）
*   **优化 #2**: 空状态提示文案优化

### ✅ 历史版本
*   **v0.9.0**: 定时扫描配置、WAL 模式、扫描锁
*   **v0.9.1**: 仪表盘 Chart.js 图表
*   **v0.9.2**: CSV 导出功能

### ⏭️ 下一步计划
*   **v0.10.0 候选**: Excel 导出、资产趋势图表、API 文档。

---

## 📁 项目结构
```
/root/network-discovery/
├── app/main.py           # version: 0.9.3
├── static/js/app.js      # 版本号动态加载
├── static/css/style.css  # bento-grid: 4 columns
├── scan.html             # 输入校验
├── index.html            # 空状态优化
└── assets.html           # 空状态优化
```

---

## 🛠️ 开发规范

1. **Git 分支**: 在 `dev/*` 分支开发，完成后合并到 `main`
2. **版本动态化**: 前端通过 /health 动态加载版本号
3. **CSS 响应式**: 桌面 4 列，平板 2 列，移动端 1 列
